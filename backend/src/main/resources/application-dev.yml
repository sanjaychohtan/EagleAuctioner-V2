spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eagleauctioner
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true


logging:
  level:
    com.eagleauctioner: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
